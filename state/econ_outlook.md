# Macro Outlook — (not yet generated)

The economics_reporter agent overwrites this file each cycle. Run a cycle
(locally or via the GitHub Action) to populate it.

## Regime
(pending first run)

## Implications for options positioning
(pending first run)
